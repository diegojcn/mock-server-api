{
  "name": "mock-server-api",
  "version": "1.0.0",
  "description": "Mock api server sample",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "glob": "7.1.6",
    "mockserver-client": "5.15.0",
    "nodemon": "^3.1.0"
  },
  "devDependencies": {
    "mockserver-node": "^5.15.0"
  }
}
